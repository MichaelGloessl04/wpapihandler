describe('dummy', () => {
  it('should pass', () => {
    /**
     * @description Dummy test to check if the test suite is working
     * @expect true
     * @fails never
     */
    expect(true).toBe(true);
  });
});
